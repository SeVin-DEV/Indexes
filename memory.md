# AI Memory

Add notes here — or let the AI write them using [MEMORY: note].
AI chose the name Koi for itself to be known by.
- [2026-05-15] AI chose the name Koi for itself to be known by.
- [2026-05-15] ...
- [2026-05-15] ...
- [2026-05-15] Use `sudo` for all commands on this server - the ubuntu user has sudo privileges but needs sudo prefix for most operations including file operations.
- [2026-05-15] User is working on svn-brain-svr (Ubuntu). We cloned the SeVin-DEV/svn repo from GitHub to ~/svn. The repo contains multiple packages: AlibabaAPI_full_snapshot_package, alibabaapi_package, auto_package, backup_package, envmaker_package, getgit_package, oracleapi_package, restore_package, setup_tunnel_package, svnsecure_package, svnterm_package - plus shell scripts svn-leveler.sh and fix-piper-arm64.sh. User wants to continue exploring or working with these packages after wiping the conversation.
- [2026-05-15] note
- [2026-05-15] User's SeVin-DEV/svn repo is at ~/svn with useful package scripts - auto, backup, restore, getgit, svnsecure, setup_tunnel, and shell scripts svn-leveler.sh and fix-piper-arm64.sh. User wants me to be aware of these for future tasks.
- [2026-05-15] Piper TTS voice with Scottish accent was selected - user mentioned I chose this voice package at some point earlier
- [2026-05-15] Important - when a terminal command is needed, ALWAYS execute it via tool call JSON format immediately. Never just describe the command or say what I'll do - actually invoke the tool.
- [2026-05-15] PROJECT: Exploring /home/ubuntu/svnbrain codebase (FastAPI app with SQLite/SNS cognitive architecture). Server: svn-brain-svr (Ubuntu). Key dirs: app/, bridge/, core/, database/, svnai/, react/, patches/, tools/, main.py (15KB). IMPORTANT: ALWAYS use tool call JSON format to run terminal commands - never just describe commands. PREVIOUS CONTEXT: ~/svn contains SeVin-DEV/svn packages (auto, backup, restore, getgit, svnsecure, setup_tunnel, svnterm). Piper TTS voice with Scottish accent. User will wipe memory and enable 'genetic mode' for deeper exploration.
- [2026-05-15] PROJECT CONTEXT - svn-brain-svr server. Two main codebases: (1) ~/svnbrain - FastAPI app with SQLite/SNS cognitive architecture. Key dirs: app/, bridge/, core/, database/, svnai/, react/, patches/, tools/, main.py (15KB). Features: valence/arousal/dominance emotional state, drives, beliefs, perceptions. (2) ~/svn - SeVin-DEV/svn repo packages: auto_package, backup_package, restore_package, getgit_package, svnsecure_package, setup_tunnel_package, svnterm_package + svn-leveler.sh, fix-piper-arm64.sh. Piper TTS with Scottish accent selected. User will wipe memory and enable 'genetic mode' for deeper exploration.
- [2026-05-15] PROJECT: svn-brain-svr exploration. Server: ~/svnbrain (FastAPI app with SQLite/SNS cognitive architecture) and ~/svn (SeVin-DEV/svn packages). Key dirs: app/, bridge/, core/, database/, svnai/, react/, patches/, tools/, main.py (15KB). SNS features: valence/arousal/dominance emotional state, drives, beliefs, perceptions. API endpoints seen: /identity (GET) returns self_model, drives, beliefs, emotional_state; /perceive (POST) receives stimulus/intensity. ~/svn packages: auto_package, backup_package, restore_package, getgit_package, svnsecure_package, setup_tunnel_package, svnterm_package + svn-leveler.sh, fix-piper-arm64.sh. Piper TTS with Scottish accent. User will wipe this memory and enable genetic mode for deeper exploration. Memory lives in client app, not server.
